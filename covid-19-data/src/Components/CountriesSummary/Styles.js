import { makeStyles } from "@material-ui/core/styles";
const useStyles = makeStyles({
  root: {
    width: "100%",
  },
  container: {
    maxHeight: 440,
  },
  link: {
    textTransform: "none",
    textDecoration: "none",
    color: "black",
  },
});
export default useStyles;
